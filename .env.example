# Copy to .env and fill in real values for local development.
# Generate a SECRET_KEY with:
#   python -c "import secrets; print(secrets.token_urlsafe(50))"

DEBUG=True
SECRET_KEY=local-dev-only-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
STUDIO_USERNAME=studio
STUDIO_PASSWORD=studio

# Optional — defaults shown
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
LOG_LEVEL=INFO

# Production (Render sets these automatically via render.yaml)
# DEBUG=False
# ALLOWED_HOSTS=tpradeep.com.np,www.tpradeep.com.np,.onrender.com
# CSRF_TRUSTED_ORIGINS=https://tpradeep.com.np,https://www.tpradeep.com.np
# DATABASE_URL=<from Render Postgres>
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True
# CONTACT_NOTIFICATION_EMAIL=you@example.com
